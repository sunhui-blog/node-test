// npm list

// npm list -g 也一样，但适用于全局安装的软件包。

// 若要仅获取顶层的软件包（基本上就是告诉 npm 要安装并在 package.json 中列出的软件包），则运行 npm list --depth=0

// 指定名称来获取特定软件包的版本 npm list cowsay

// 适用于安装的软件包的依赖 npm list minimist

// npm 仓库上最新的可用版本 npm view cowsay version 1.4.0
